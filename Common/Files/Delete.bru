meta {
  name: Delete
  type: http
  seq: 3
}

delete {
  url: {{HOST_COMMON}}/files/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
